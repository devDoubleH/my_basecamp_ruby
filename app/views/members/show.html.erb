<div class="container">
  <div class="row">
    <div class="col-8">
      <div class="members-container">
        <p>
          <strong>Name:</strong>
          <%= @member.name %>
        </p>

        <p>
          <strong>Email:</strong>
          <%= @member.email %>
        </p>

        <%= link_to 'Edit', edit_project_member_path(@project, @member), class:"btn btn-primary" %>
        <%= link_to 'Back', project_members_path(@project), class:"btn btn-primary" %>
      </div>
    </div>
  </div>
</div>
